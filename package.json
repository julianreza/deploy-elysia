{
  "name": "deploy-elysia",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "build": "bun run build vercel-build.ts",
    "start": "bun run dist/index.ts"
  },
  "dependencies": {
    "@elysiajs/node": "^1.2.6",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}