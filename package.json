{
  "name": "deploy-elysia",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch api/index.ts",
    "build": "bun build --target bun --outfile ./build/index.js ./api/index.ts",
    "start": "bun run build/index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@vercel/node": "^5.1.14",
    "elysia": "latest"
  },
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  }
}